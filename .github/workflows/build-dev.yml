name: Build wheels (dev)

on:
  # dev requires a PR; build the full wheel matrix before merging.
  # Post-merge tests remain in test-dev.yml. For an explicit rebuild use
  # the existing Build wheels workflow (build.yaml) with ref=dev.
  pull_request:
    branches: [dev]
    paths-ignore:
      - '**.md'
      - '**.rst'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  wheels:
    name: Wheels / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      - uses: ./.github/actions/build-wheels
